# TriFuel v2
Focus: race nutrition, hydration/sodium, race-day timing, dynamic stretching, static stretching, and guided mobility timers.
Training plans/workouts are intentionally excluded because the user uses a separate training app.
Upload all files to the GitHub repository root and enable GitHub Pages from `main` + `/ (root)`.
